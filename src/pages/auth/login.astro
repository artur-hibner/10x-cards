---
// Przekierowanie na stronę główną, zachowując parametr redirect jeśli istnieje
const redirectParam = Astro.url.searchParams.get("redirect");
const targetUrl = redirectParam ? `/?redirect=${encodeURIComponent(redirectParam)}` : "/";
return Astro.redirect(targetUrl);
---
